from setuptools import setup, find_packages

setup(
    name="bank_telemarketing",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "pandas",
        # Add other dependencies here
    ],
)